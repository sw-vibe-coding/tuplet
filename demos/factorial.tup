# factorial.tup -- recursive verb. Mints `fact` and uses
# self-reference inside the body. Demonstrates that user-
# defined verbs are first-class.

*fact (n) -> (r) <-
  if n <= 1 then
    1
  else
    n * fact (n - 1)
  end

*answer <- fact 5
# expected: answer = 120
