Start the `tuplet-forth-emit` saga with a narrow parser-backed emitter scaffold. Implement deterministic Forth text emission for the current IR subset only: tuple declarations, scalar store declarations, tuple loads, `PUSH_INT`, `PUSH_PCT`, `STORE`, and shallow `CALL`. Add parser-backed reg-rs baselines for coord2 tuple assignment and plot call-splicing. Do not run or depend on source-backed IR while `sw-cor24-ocaml#33` is open, do not edit sibling repos, and do not implement a full prelude or interpreter.
