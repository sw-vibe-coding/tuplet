Create the checker saga scaffold without changing parser behavior. Add src/checker.ml plus a minimal checker dump driver over hand-built parser AST nodes if that matches current source layout. Support a deterministic pass dump for one tuple signature followed by a matching tuple-pattern assignment, and deterministic fail dumps for unbound RHS name and assignment arity mismatch. Add docs/checker.md describing the first checker subset and parser deferrals. Add focused reg-rs baselines for pass and fail cases. Keep IR/interpreter/Forth emission out of scope. Finish with relevant parser/checker regressions, markdown checks, AgentRail completion, commit, and push Tuplet repo changes only.