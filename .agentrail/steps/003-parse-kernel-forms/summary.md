Implemented kernel-form parsing beyond one-token statements. AST dumps now include statement kinds and shallow structured groups for parens/braces. The parser recognizes assignment streams, mint-prefixed streams, syntax declaration heads split into template and expansion groups, skips comments, preserves commas/slots, and reports deterministic group errors. Added assignment, syntax-head, and group-error drivers and reg-rs baselines. Also fixed run-ml.sh temporary file creation for reliable concurrent multi-file runs.
