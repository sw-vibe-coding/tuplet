Extend the Tuplet IR scaffold beyond the coord2 assignment baseline without implementing Forth emission or interpretation. Add parser-backed IR coverage for shallow call-site tuple splicing using the checker-complete call AST shape, and add a checker-rejection/fail-fast IR baseline. Keep source-backed IR lowering gated on sw-cor24-ocaml#33; do not raise Tuplet runtime limits or commit failing source-backed baselines. Update `docs/ir.md` if the dump shape changes.
