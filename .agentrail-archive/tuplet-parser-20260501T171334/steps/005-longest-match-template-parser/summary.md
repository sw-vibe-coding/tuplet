Implemented the first registry-driven syntax matcher. Later parses in the same process consult registered templates before kernel fallback, producing deterministic `syntax-match` AST dumps with mode, template, slots, and expansion groups. Matching now covers basic registered syntax use, longest-match wins, and first-declared wins on equal match length, with reg-rs baselines for each case.
