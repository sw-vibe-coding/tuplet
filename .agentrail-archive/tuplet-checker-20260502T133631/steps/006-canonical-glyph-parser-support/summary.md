Implemented canonical glyph parsing on the memory-backed UTF-8 source path: shell parens ⎛ ⎠ fold to tuple delimiters, subscript arity digits such as ₂ fold inside identifiers, and the full ───‣ sequence folds to RARROW. Added canonical tuple and verb signature source fixtures plus reg-rs baselines. Documented that the legacy UART low-byte path still cannot carry ─ because its low byte is the lexer sentinel. Checker source handoff remains blocked on sw-cor24-ocaml#30.