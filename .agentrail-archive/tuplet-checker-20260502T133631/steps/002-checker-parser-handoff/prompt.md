Wire the checker scaffold to real parser output for the currently supported parser subset. Add a checker driver that parses source or parser token streams through src/parser.ml before checking, starting with coord2 signature plus tuple destructuring and a call-arity case if parser statement sequencing supports it. Preserve the hand-built AST baselines as checker unit coverage. Add/adjust reg-rs baselines and docs/checker.md to describe parser-backed checker acceptance. Keep IR/interpreter/Forth emission out of scope. Finish with relevant parser/checker regressions, markdown checks, AgentRail completion, commit, and push Tuplet repo changes only.