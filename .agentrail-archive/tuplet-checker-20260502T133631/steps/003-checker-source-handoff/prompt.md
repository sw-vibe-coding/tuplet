Wire checker acceptance to real source fixtures through lexer -> parser -> checker for the currently supported subset. Start with coord2 signature plus tuple destructuring and, if feasible, a call arity fixture using current parser sequencing. Preserve parser-token and hand-built AST baselines. Document any source/statement-boundary limitations precisely in docs/checker.md or parser docs. Keep IR/interpreter/Forth emission out of scope. Finish with relevant lexer/parser/checker regressions, markdown checks, AgentRail completion, commit, and push Tuplet repo changes only.